# Required: set one provider API key
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: backend port (default 3001)
PORT=3001

# Optional: CORS allowed origins (comma-separated)
APP_ORIGIN=http://localhost:3000

# Optional: path to capability map JSON
CAPABILITY_MAP_PATH=../n.codes.capabilities.json
